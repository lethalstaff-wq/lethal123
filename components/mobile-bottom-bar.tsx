"use client"

// Bottom-bar duplicated navbar actions on mobile and only distracted from the
// hero. Removed per user feedback; navbar hamburger is the canonical nav path.
// Keeping the component as a no-op so existing imports don't break.
export function MobileBottomBar() {
  return null
}
