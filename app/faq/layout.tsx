import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "FAQ | Lethal Solutions",
  description: "Frequently asked questions about DMA cheats, HWID spoofers, custom firmware, payment methods, and setup guides.",
}

export default function FAQLayout({ children }: { children: React.ReactNode }) {
  return children
}
