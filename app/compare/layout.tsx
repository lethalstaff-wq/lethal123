import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Compare Products | Lethal Solutions",
  description: "Compare DMA cheats, spoofers, and firmware side by side. Find the right product for your setup and budget.",
}

export default function CompareLayout({ children }: { children: React.ReactNode }) {
  return children
}
